# ============================================================
# Bluetooth Positioning System - 設定ファイル（v5対応）
# Surface 設置時にこのファイルを編集してください
# ============================================================

# -----------------------------------------------
# このSurface の識別情報
# -----------------------------------------------

# 設置されている教室のブースID（企画書v5 §9.2 命名規則に従う）
# 例: "class1-1", "club-art", "eat-car-1"
BOOTH_ID = "class1-1"

# 担当者識別文字列（changeLogsの operatorId として記録される）
# 固定文字列。boothId と連動させる（質問54でA採用）
OPERATOR_ID = f"bt-{BOOTH_ID}"

# -----------------------------------------------
# Bluetooth スキャン設定
# -----------------------------------------------

# スキャン間隔（秒）※ config コレクションの scanIntervalSeconds と統一（v5: 90秒）
SCAN_INTERVAL_SECONDS = 90

# 1回のスキャン時間（秒）
SCAN_DURATION_SECONDS = 10

# -----------------------------------------------
# 送信先 API（管理用Vercel）
# ※ Shoki の実装完了後に確定するURLをここに入力する
# -----------------------------------------------

# POST 送信先エンドポイント（企画書v5 §10.3）
API_ENDPOINT = "https://【管理用Vercelのドメイン】/api/booth/bluetooth"

# API 認証用トークン（必要な場合。Shokiと要相談）
API_AUTH_TOKEN = "【未確定】"

# リクエストタイムアウト（秒）
API_TIMEOUT_SECONDS = 10

# -----------------------------------------------
# ローカル動作確認用設定（質問56でA採用：参考用に残す）
# 本番送信では使用しない。Surface単体テスト時の目安として使う。
# -----------------------------------------------

# 朝テストで計測した baselineMax（事前テスト後に更新）
LOCAL_PREVIEW_BASELINE_MAX = 20

# 切り替え時刻（事前テスト後に更新: "HH:MM" 形式）
LOCAL_PREVIEW_SWITCH_TIME = "09:30"

# 標準偏差の閾値
LOCAL_PREVIEW_STDDEV_THRESHOLD = 2.5

# 混雑レベル判定割合（企画書v5 §8.4 仮置き値）
LOCAL_PREVIEW_RATIOS = {
    1: (0,   15),   # 非常に閑散
    2: (16,  35),   # 閑散
    3: (36,  55),   # 通常
    4: (56,  75),   # 混雑
    5: (76, 100),   # 非常に混雑
}

# -----------------------------------------------
# ローカルログ設定（送信失敗時のフェイルオーバー用）
# -----------------------------------------------

LOG_DIR = "../bluetooth-positioning-system-data/logs"
